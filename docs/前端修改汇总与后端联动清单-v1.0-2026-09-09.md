```markdown
# CTCP 前端修改汇总与后端联动清单

| 项目 | 内容 |
|---|---|
| 版本 | v1.0 |
| 日期 | 2026-09-08 ~ 2026-09-09 |
| 修改人 | B组（前端） |
| 涉及范围 | 跑腿任务、个人空间、管理员面板、发现首页、任务详情页、发布任务页 |
| 基线文档 | `docs/系统测试问题汇总 · B组（跑腿个人空间管理员）.md` |


## 1. 整体修改概述

本文档涵盖本次「系统测试问题汇总 · B组」中所有**前端已完成的修改**，以及需要后端配合的接口联动事项。前端修改涉及 12 个文件，覆盖跑腿任务大厅、任务详情、个人空间（普通用户/管理员）、发现首页、任务发布等核心页面。

---

## 2. 已完成的纯前端修改

### 2.1 跑腿任务大厅：移除「跑腿任务」标签和四角星

| 项目 | 内容 |
|---|---|
| 问题编号 | B组 · 一.1 |
| 涉及文件 | `web/js/task.js` |
| 改动说明 | 删除 `card()` 函数中渲染 `.visual-label` 和 `.visual-doodle` 的两行代码 |
| 验收要点 | 任务卡片图片上不再显示「跑腿任务」文字标签和 ✦ 四角星装饰 |

### 2.2 任务详情页：返回按钮移到右上角

| 项目 | 内容 |
|---|---|
| 问题编号 | B组 · 一.4 |
| 涉及文件 | `web/task-detail.jsp`、`web/js/task-detail.js` |
| 改动说明 | JSP 中在 `workspace__actions` 内新增 `<div data-detail-back-container>`；JS 新增 `renderReturnButton()` 将返回链接渲染到右上角容器 |
| 验收要点 | 返回按钮位于右上角，与「发布任务」按钮水平对齐 |

### 2.3 任务详情页：隐藏「发布任务」按钮

| 项目 | 内容 |
|---|---|
| 问题编号 | 新增任务（非原清单） |
| 涉及文件 | `web/js/task-detail.js` |
| 改动说明 | 新增 `hidePublishButton()` 函数，在 `render()` 和 `load()` 各分支中调用，隐藏 `.workspace__actions .market-publish-button` |
| 验收要点 | 任务详情页右上角不显示「发布任务」按钮，「返回xxx」按钮正常显示 |

### 2.4 管理员页面细节完善

| 项目 | 内容 |
|---|---|
| 问题编号 | B组 · 一.2 |
| 涉及文件 | `web/profile-admin.jsp`、`web/js/admin.js`、`web/css/profile.css` |
| 改动说明 | JSP：Tab 按钮移除 Emoji，标题改为「管理员面板」；JS：按钮移除 Emoji，删除重复小标题，编辑模式改用 `user-edit-form`；CSS：Tab 和状态筛选改为圆角按钮，新增编辑模式样式 |
| 验收要点 | 管理员页面无多余 Emoji，Tab 为纯文字圆角按钮，编辑表单样式与页面风格统一 |

### 2.5 个人空间：「我的记录」标题与 Tab 间距

| 项目 | 内容 |
|---|---|
| 问题编号 | B组 · 一.3 |
| 涉及文件 | `web/css/profile.css` |
| 改动说明 | 新增 `.profile-records .section-heading { margin-bottom: 16px; }` |
| 验收要点 | 「我的记录」标题与四个 Tab 按钮之间间距适中，无视觉拥挤 |

### 2.6 个人空间：修改密码「正在保存」竖排显示

| 项目 | 内容 |
|---|---|
| 问题编号 | B组 · 一.5 |
| 涉及文件 | `web/css/profile.css` |
| 改动说明 | `.profile-card .publish-actions .primary-action` 增加 `white-space: nowrap; justify-content: center;` |
| 验收要点 | 点击保存后「正在保存...」文字横向单行显示 |

### 2.7 个人空间：头像在侧边栏未同步

| 项目 | 内容 |
|---|---|
| 问题编号 | B组 · 一.6 |
| 涉及文件 | `web/js/profile.js` |
| 改动说明 | 新增 `syncSidebarAvatar()` 函数，上传/恢复头像时同步更新侧边栏 `[data-user-avatar]` |
| 验收要点 | 上传头像后侧边栏头像同步更新为图片；恢复默认后同步显示首字母 |

### 2.8 网络请求失败提示优化

| 项目 | 内容 |
|---|---|
| 问题编号 | B组 · 一.7 |
| 涉及文件 | `web/js/api.js` |
| 改动说明 | 错误信息从「无法连接后端服务，请确认 8081 端口已启动」改为「网络连接失败，请检查网络后刷新重试」 |
| 验收要点 | 网络异常时普通用户看到友好提示，不包含技术细节 |

### 2.9 手机号字段校验（个人空间）

| 项目 | 内容 |
|---|---|
| 问题编号 | B组 · 二.3 |
| 涉及文件 | `web/profile-user.jsp`、`web/js/profile.js` |
| 改动说明 | JSP：电话输入框增加 `type="tel" maxlength="11" inputmode="numeric" pattern="[0-9]*"`；JS：`saveProfile()` 增加 `if (phone && !/^\d{11}$/.test(phone))` 校验 |
| 验收要点 | 填写非 11 位数字时提示「电话必须为 11 位数字」 |

### 2.10 超大金额限制（跑腿任务）

| 项目 | 内容 |
|---|---|
| 问题编号 | B组 · 四.1 |
| 涉及文件 | `web/task-publish.jsp`、`web/js/task-publish.js` |
| 改动说明 | JSP：金额输入框增加 `max="100000"`；JS：提交校验增加 `if (amount > 100000)` 判断 |
| 验收要点 | 输入超过 100,000 元时提示「跑腿金额不能超过 100,000 元」 |


## 3. 需要后端配合的联动事项

### 3.1 首页跑腿待办

| 项目 | 内容 |
|---|---|
| 问题编号 | B组 · 二.2 |
| 涉及文件 | `web/js/home.js` |
| 前端改动 | 新增 `taskTodoItem()` 和 `buildTaskTodos()` 函数，`renderTodos()` 合并交易待办 + 跑腿待办，`loadHome()` 新增两个请求 |
| 后端需求 | 实现以下两个接口 |
| 验收要点 | 首页待办区同时显示交易待办和跑腿待办，状态映射见下表 |

#### 3.1.1 接口：获取我发布的任务

```http
GET /api/me/tasks?type=published&page=1&size=50
```

**认证**：需要（当前用户）

**权限**：返回当前用户发布的所有任务

**请求参数**

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `type` | string | 是 | 固定值 `published` |
| `page` | integer | 否 | 页码，默认 1 |
| `size` | integer | 否 | 每页大小，默认 50，最大 100 |

**成功响应** `200 OK`

```json
{
  "code": 0,
  "msg": "ok",
  "data": {
    "list": [
      {
        "id": 101,
        "title": "帮取南区快递",
        "status": "open",
        "amount": 5.00,
        "publisherName": "张三",
        "createdAt": "2026-09-08 10:00:00"
      }
    ],
    "total": 1,
    "page": 1,
    "size": 50
  }
}
```

#### 3.1.2 接口：获取我接取的任务

```http
GET /api/me/tasks?type=accepted&page=1&size=50
```

**认证**：需要（当前用户）

**权限**：返回当前用户作为接取者的所有任务

**请求参数**

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `type` | string | 是 | 固定值 `accepted` |
| `page` | integer | 否 | 页码，默认 1 |
| `size` | integer | 否 | 每页大小，默认 50，最大 100 |

**成功响应** `200 OK`

```json
{
  "code": 0,
  "msg": "ok",
  "data": {
    "list": [
      {
        "id": 102,
        "title": "图书馆占座并带份早餐",
        "status": "accepted",
        "amount": 8.00,
        "publisherName": "李四",
        "createdAt": "2026-09-08 09:00:00"
      }
    ],
    "total": 1,
    "page": 1,
    "size": 50
  }
}
```

#### 3.1.3 跑腿待办状态映射（前端已实现）

前端 `buildTaskTodos()` 函数按以下规则生成待办项：

| 角色 | 任务状态 | 显示状态 | 是否需要操作 |
|------|---------|---------|-------------|
| 发布者 | `accepted` | 等待送达 | ❌ 等待接取者送达 |
| 发布者 | `delivered` | 待确认 | ✅ 点击确认完成 |
| 接取者 | `accepted` | 待送达 | ✅ 点击标记送达 |
| 接取者 | `delivered` | 已送达 | ❌ 等待发布者确认 |

- `open`、`completed` 状态不进入待办列表。
- 待办项点击后跳转至 `task-detail.jsp?taskId=${task.id}`。

**当前状态**：⏳ 待后端实现接口

---

### 3.2 驳回后发布者查看任务详情

| 项目 | 内容 |
|---|---|
| 问题编号 | B组 · 二.1 |
| 涉及文件 | `web/js/task-detail.js` |
| 前端改动 | `load()` 中公开接口失败时尝试调用私有接口 `/me/tasks/{taskId}`；渲染时检测 `auditStatus === "rejected"` 显示驳回横幅和修改入口 |
| 后端需求 | ① 新增 `GET /api/me/tasks/{taskId}` 接口<br>② 修改 `GET /api/public/tasks/{taskId}` 权限逻辑 |
| 验收要点 | 发布者能看到被驳回任务的驳回理由和「修改后重新提交」按钮 |

#### 3.2.1 新增接口：获取当前用户发布的单个任务

```http
GET /api/me/tasks/{taskId}
```

**认证**：需要（当前用户）

**权限**：仅发布者本人可访问，非发布者返回 `403`

**路径参数**

| 参数 | 类型 | 说明 |
|---|---|---|
| `taskId` | integer | 任务 ID |

**成功响应** `200 OK`

```json
{
  "code": 0,
  "msg": "ok",
  "data": {
    "id": 103,
    "title": "代拿药品",
    "description": "帮我去校医院取一下药，药单在发布者手上",
    "pickup": "校医院",
    "delivery": "7栋宿舍515",
    "amount": 10.00,
    "contact": "QQ: 12345678",
    "status": "open",
    "auditStatus": "rejected",
    "auditRemark": "任务描述不够清晰，请补充取药具体信息，比如是否需要处方单",
    "publisherId": 1,
    "publisherName": "张三",
    "createdAt": "2026-09-07 08:00:00",
    "deadline": "2026-09-10 18:00:00"
  }
}
```

#### 3.2.2 修改接口：公开任务详情

```http
GET /api/public/tasks/{taskId}
```

**认证**：可选

**路径参数**

| 参数 | 类型 | 说明 |
|---|---|---|
| `taskId` | integer | 任务 ID |

**权限逻辑修改**：

```
IF task.auditStatus = 'approved' THEN
    → 返回完整任务详情（所有人可见，原有逻辑不变）
ELSE IF task.auditStatus IN ('pending', 'rejected') THEN
    currentUser = 从 Session 获取当前用户
    IF currentUser IS NOT NULL AND currentUser.id = task.publisherId THEN
        → 返回完整任务详情（包含 auditStatus 和 auditRemark）
    ELSE
        → 返回 404（任务不存在或已删除）
    END IF
ELSE
    → 返回 404
END IF
```

**成功响应** `200 OK`（与现有 `/public/tasks/{id}` 结构一致，但待审核/已驳回状态额外包含审核字段）

```json
{
  "code": 0,
  "msg": "ok",
  "data": {
    "id": 103,
    "title": "代拿药品",
    "status": "open",
    "auditStatus": "rejected",
    "auditRemark": "任务描述不够清晰，请补充取药具体信息",
    "publisherId": 1,
    "publisherName": "张三"
  }
}
```

**当前状态**：⏳ 待后端实现

---

### 3.3 管理员删除任务限制

| 项目 | 内容 |
|---|---|
| 问题编号 | B组 · 三.1 |
| 涉及文件 | `web/js/admin.js`、后端 `AdminController.java`、`AdminService.java` |
| 前端改动 | 删除按钮仅在 `auditStatus === "approved" && status === "completed"` 时显示，否则显示灰色「不可删除」标签 |
| 后端需求 | `AdminService.deleteTask()` 中增加状态校验 |
| 安全说明 | 前端隐藏按钮仅提升用户体验，后端校验是真正的安全防护 |

#### 3.3.1 修改接口：管理员删除任务

```http
DELETE /api/admin/tasks/{taskId}
```

**认证**：需要（管理员）

**权限逻辑修改**：

```
Task task = taskDao.getById(taskId);
IF task IS NULL THEN
    → 返回 404
END IF
IF task.status != 'completed' THEN
    → 返回 400 或 409，msg: "只能删除状态为「已完成」的任务"
END IF
// 执行软删除
taskDao.updateStatus(taskId, "deleted");
```

**响应示例（失败时）**：

```json
{
  "code": 400,
  "msg": "只能删除状态为「已完成」的任务",
  "data": null
}
```

**当前状态**：⏳ 待后端实现

---

### 3.4 管理员审核查看任务详情

| 项目 | 内容 |
|---|---|
| 问题编号 | B组 · 三.2 |
| 涉及文件 | `web/js/admin.js`、后端 `TaskController.java` |
| 前端改动 | 每个任务项增加蓝色「查看详情」按钮，点击在新窗口打开任务详情页 |
| 后端需求 | 修改 `GET /api/public/tasks/{taskId}` 增加管理员权限 |
| 验收要点 | 管理员点击「查看详情」能正常看到待审核任务的完整信息 |

#### 3.4.1 修改接口：公开任务详情（管理员豁免）

```http
GET /api/public/tasks/{taskId}
```

**认证**：可选

**权限逻辑补充**：

```
currentUser = 从 Session 获取当前用户
IF currentUser IS NOT NULL AND currentUser.role = 'admin' THEN
    → 返回完整任务详情（允许查看待审核/已驳回任务）
END IF
// 否则走 3.2.2 的普通用户逻辑
```

**设计说明**：

| 用户类型 | `auditStatus` | 结果 |
|---|---|---|
| 管理员 | 任意状态 | ✅ 允许查看（完整详情） |
| 发布者 | `pending` / `rejected` | ✅ 允许查看（含审核字段） |
| 普通用户 | `pending` / `rejected` | ❌ 404 |
| 所有用户 | `approved` | ✅ 允许查看 |

**当前状态**：⏳ 待后端实现


## 4. 已跳过或暂缓的任务

| 任务 | 原因 |
|---|---|
| 未登录请求管理接口跳转生硬（B组 · 一.8） | 用户决定不改 |
| 侧边栏彩色小圆点 | 用户决定暂缓 |


## 5. 后端接口清单汇总

| # | 接口 | 方法 | 用途 | 类型 | 状态 |
|---|------|------|------|------|------|
| 1 | `/api/me/tasks?type=published` | GET | 首页跑腿待办 — 发布者视角 | 新增 | ⏳ 待实现 |
| 2 | `/api/me/tasks?type=accepted` | GET | 首页跑腿待办 — 接取者视角 | 新增 | ⏳ 待实现 |
| 3 | `/api/me/tasks/{taskId}` | GET | 驳回后发布者查看任务详情 | 新增 | ⏳ 待实现 |
| 4 | `/api/public/tasks/{taskId}` | GET | ① 驳回状态对发布者返回详情<br>② 管理员查看所有状态任务 | 修改 | ⏳ 待修改 |
| 5 | `/api/admin/tasks/{id}` | DELETE | 限制只能删除 `status="completed"` 的任务 | 修改 | ⏳ 待修改 |


## 6. 错误码汇总

| HTTP | code | msg | 场景 |
|---:|---:|---|---|
| `200` | `0` | `ok` | 成功 |
| `400` | `400` | `只能删除状态为「已完成」的任务` | 管理员删除非已完成任务 |
| `403` | `403` | `无权限查看该任务` | 非发布者访问私有任务接口 |
| `404` | `404` | `任务不存在或已删除` | 任务不存在或无权访问 |
| `401` | `401` | `请先登录` | 未登录访问需要认证的接口 |


## 7. 已修改文件清单（共 12 个）

| 文件 | 涉及任务 |
|---|---|
| `web/js/task.js` | 移除「跑腿任务」标签和四角星 |
| `web/task-detail.jsp` | 返回按钮容器 |
| `web/js/task-detail.js` | 返回按钮移到右上角、隐藏发布按钮、驳回状态展示 |
| `web/profile-admin.jsp` | 管理员页面 Tab/标题优化 |
| `web/js/admin.js` | 管理员页面 Emoji 移除、用户编辑样式、删除按钮条件显示、查看详情按钮 |
| `web/css/profile.css` | 圆角按钮、标题间距、正在保存横排、编辑模式样式 |
| `web/js/profile.js` | 侧边栏头像同步、手机号校验 |
| `web/js/api.js` | 网络错误提示优化 |
| `web/profile-user.jsp` | 手机号输入框限制 |
| `web/task-publish.jsp` | 金额输入框 max 限制 |
| `web/js/task-publish.js` | 金额提交校验 |
| `web/js/home.js` | 首页跑腿待办（前端逻辑已完成，待后端接口） |


## 8. 验收要点

1. 跑腿任务大厅卡片图片上方不再显示「跑腿任务」标签和四角星。
2. 任务详情页返回按钮位于右上角「发布任务」按钮左侧，且「发布任务」按钮本身已隐藏。
3. 管理员页面 Tab 为纯文字圆角按钮，编辑模式表单风格与页面统一。
4. 个人空间「我的记录」标题与 Tab 按钮间距适中，修改密码时「正在保存」横向显示。
5. 上传头像后侧边栏同步更新，恢复默认后同步显示首字母。
6. 网络异常时显示友好提示，不包含端口号等技术细节。
7. 个人空间电话输入框限制 11 位数字，提交时校验。
8. 发布任务金额最大 100,000 元，超出时提示。
9. 首页待办区显示跑腿待办（需后端接口就绪后验证）。
10. 发布者能查看被驳回任务的驳回理由并跳转修改（需后端接口就绪后验证）。
11. 管理员列表仅「已完成」任务显示删除按钮，其他显示「不可删除」（需后端校验同步）。
12. 管理员审核列表显示「查看详情」按钮，可查看完整任务信息（需后端开放权限）。
```